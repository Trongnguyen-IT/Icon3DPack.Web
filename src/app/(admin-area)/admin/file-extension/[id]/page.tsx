export default function FileExtensionDetail({ params }: { params: { id: string } }) {
	return <div>FileExtension details</div>
}

// export async function generateStaticParams({ params }: { params: { id: string } }) {
// 	return [params]
// }
